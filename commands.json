{
  "getEvents": {
    "type": "object",
    "description": "Get Stripe events",
    "properties": {
      "stripe": {
        "$ref": "#/definitions/stripe"
      }
    },
    "required": [
      "stripe"
    ]
  },
  ".definitions": {
    "stripe": {
      "type": "object",
      ".linchpin": "Integrations/stripe",
      "properties": {
        "name": {
          "type": "string",
          "title": "Name"
        },
        "api_key": {
          "type": "string",
          "title": "Api key"
        }
      },
      "required": [
        "name",
        "api_key"
      ]
    }
  }
}